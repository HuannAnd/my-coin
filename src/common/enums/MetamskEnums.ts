 export enum MetamaskErrorResponseEnum {
  METAMASK_ERROR = -32002
 }

 export enum MetamaskConnectionStatusEnum {
  WAITING = "waiting",
  TIMEOUT = "timeout",
  REFUSED = "disconnected",
  CONNECTED = "connected"

 }