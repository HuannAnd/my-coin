enum MediaQueriesEnum {
  MOBILE = "(width <= 480px)",
  TABLET = "(480px < width <= 768px)",
  DESKTOP_LOW_RESOLUTION = "(768px < width <= 1024px)",
  DESKTOP_HIGH_RESOLUTION = "(1200px < width)"

  
}

export default MediaQueriesEnum